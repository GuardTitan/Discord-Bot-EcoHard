Bot_name = "EcoHard";
Bot_1 = "GuardTitan";
Bot_2 = Bot_name;

import androidhelper as root_run

for root in root_run {
  root = root.list()
  for n in root {
    @stand.root()
    j1=root.name=Bot_name;
    j2=root.id=991828173;
    @stand.root(end)
    }
}

 
@stand.root(-1, 0, 1, root.id(n)) {root_run};
  end;
end;
